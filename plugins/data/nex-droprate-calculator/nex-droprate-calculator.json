{"commit":"48217ba0e2915e9553c922d73034c5f2866fc4a7","repository":"https://github.com/Worley03/nex-droprate-calculator.git","internalName":"nex-droprate-calculator","files":[{"fileName":"NexDroprateCalculatorPluginTest.java","content":"package com.nexdropratecalculator;\n\nimport net.runelite.client.RuneLite;\nimport net.runelite.client.externalplugins.ExternalPluginManager;\n\npublic class NexDroprateCalculatorPluginTest {\n  public static void main(String[] args) throws Exception {\n    ExternalPluginManager.loadBuiltin(NexDroprateCalculatorPlugin.class);\n    RuneLite.main(args);\n  }\n}\n","filePath":"src/test/java/com/nexdropratecalculator/NexDroprateCalculatorPluginTest.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"NexDroprateCalculatorPlugin.java","content":"package com.nexdropratecalculator;\n\nimport com.google.inject.Provides;\nimport java.awt.image.BufferedImage;\nimport java.io.FileWriter;\nimport java.io.IOException;\nimport java.io.PrintWriter;\nimport java.nio.file.Files;\nimport java.nio.file.Path;\nimport java.nio.file.Paths;\nimport java.time.LocalDateTime;\nimport java.time.format.DateTimeFormatter;\nimport java.util.concurrent.ScheduledExecutorService;\nimport javax.inject.Inject;\nimport lombok.extern.slf4j.Slf4j;\nimport net.runelite.client.RuneLite;\nimport net.runelite.api.*;\nimport net.runelite.api.events.ChatMessage;\nimport net.runelite.api.events.GameTick;\nimport net.runelite.api.events.HitsplatApplied;\nimport net.runelite.api.events.VarbitChanged;\nimport net.runelite.api.gameval.NpcID;\nimport net.runelite.api.gameval.VarbitID;\nimport net.runelite.client.callback.ClientThread;\nimport net.runelite.client.config.ConfigManager;\nimport net.runelite.client.eventbus.Subscribe;\nimport net.runelite.client.plugins.Plugin;\nimport net.runelite.client.plugins.PluginDescriptor;\nimport net.runelite.client.ui.ClientToolbar;\nimport net.runelite.client.ui.NavigationButton;\nimport net.runelite.client.util.ImageUtil;\nimport net.runelite.client.ui.overlay.OverlayManager;\n\n@Slf4j\n@PluginDescriptor(name \u003d \"Nex Droprate Calculator\")\npublic class NexDroprateCalculatorPlugin extends Plugin {\n  @Inject private Client client;\n  @Inject private ClientToolbar clientToolbar;\n  @Inject private ClientThread clientThread;\n  @Inject private ScheduledExecutorService executor;\n  protected NexDroprateCalculatorPanel panel;\n  private NavigationButton navButton;\n  @Inject private OverlayManager overlayManager;\n  @Inject private NexDroprateCalculatorOverlay overlay;\n  @Inject private NexDroprateCalculatorConfig config;\n\n  private int ownContribution \u003d 0;            // Value of own damage splats (resets each tick during the fight)\n  private int totalContribution \u003d 0;          // Value of all damage splats (resets each tick during the fight)\n  private int lastNexStatus \u003d 0;              // Default value of VarbitID.NEX_BARRIER\n\n  // Timers for scheduling actions to run in N ticks\n  private int ticksUntilDumpResults \u003d -1;     // Time in ticks until dumping fight results\n  private int ticksUntilOverlayRemoval \u003d -1;  // Time in ticks until hiding overlay\n  private int ticksUntilFightStart \u003d -1;\n  private static final int DUMP_RESULTS_TICKS \u003d 2;\n  private static final int REMOVE_OVERLAY_TICKS \u003d 50;\n  private static final int FIGHT_START_DELAY_TICKS \u003d 4; // Fight always starts 4 ticks after Nex shouts \u0027Fill my soul...\u0027\n\n  private static final String KILL_LOG_FILE_NAME \u003d \"nex-kills.log\";\n  private static final DateTimeFormatter TIMESTAMP_FORMAT \u003d DateTimeFormatter.ofPattern(\"yyyy-MM-dd HH:mm:ss\");\n\n  private boolean isNexAlive() {\n    return client.getVarbitValue(VarbitID.NEX_BARRIER) \u003d\u003d 3;\n  }\n\n  private void startFight() {\n    log.debug(\"Initializing Nex fight\");\n    panel.setCurrentRunActive(true);\n    ticksUntilOverlayRemoval \u003d -1; // Cancel potential pending countdown\n    ownContribution \u003d 0;\n    totalContribution \u003d 0;\n    overlay.setShouldRender(true);\n\n    int playerCount \u003d (int) client.getTopLevelWorldView().players().stream().count();\n    panel.startCurrentRun(playerCount);\n  }\n\n  @Override\n  protected void startUp() throws Exception {\n    log.debug(\"Starting Nex Droprate Calculator Plugin\");\n\n    // Panel\n    panel \u003d injector.getInstance(NexDroprateCalculatorPanel.class);\n    panel.init();\n    // Side panel button\n    final BufferedImage icon \u003d ImageUtil.loadImageResource(getClass(), \"icon.png\");\n    navButton \u003d NavigationButton.builder().tooltip(\"Nex Calculator\").icon(icon).panel(panel).build();\n    clientToolbar.addNavigation(navButton);\n    // Overlay\n    overlayManager.add(overlay);\n\n    // In case plugin was turned on mid-fight, check for Nex\u0027s presence\n    clientThread.invokeLater(() -\u003e {\n      log.debug(\"Invoking on client thread\");\n      if (panel \u003d\u003d null) {\n        return;\n      }\n      // Nex uses NpcID.NEX only once she\u0027s attackable, meaning fight has already started so this check is enough\n      if (client.getTopLevelWorldView().npcs().stream().anyMatch(npc -\u003e npc.getId() \u003d\u003d NpcID.NEX)) {\n        startFight();\n      }\n    });\n\n    log.debug(\"Nex Droprate Calculator Plugin started successfully\");\n  }\n\n  @Override\n  protected void shutDown() {\n    log.debug(\"Shutting down Nex Droprate Calculator Plugin\");\n\n    // Overlay (it\u0027s removed first since it relies on panel - which should not be null)\n    overlay.setShouldRender(false);\n    overlayManager.remove(overlay);\n    // Panel\n    panel \u003d null;\n    // Side panel button\n    clientToolbar.removeNavigation(navButton);\n    navButton \u003d null;\n\n    log.debug(\"Nex Droprate Calculator Plugin shut down successfully\");\n  }\n\n  @Subscribe\n  public void onVarbitChanged(VarbitChanged event) {\n    if (event.getVarbitId() \u003d\u003d VarbitID.NEX_BARRIER) {\n      int newStatus \u003d event.getValue();\n      log.debug(\"NEX_BARRIER changed: {} -\u003e {}\", lastNexStatus, newStatus);\n      // NOTE: It is possible that the varbit will change 0 -\u003e 3 if player last logged out inside Nex\u0027s arena with Nex alive\n      // hence need for the non-zero check on previous value, otherwise overlay will show up for a few seconds on login in case of 0 -\u003e 3\n      if (lastNexStatus !\u003d 0) {\n        if (lastNexStatus \u003d\u003d 3 \u0026\u0026 newStatus \u003d\u003d 2) {\n          // Nex just died (3 -\u003e 2)\n          // Schedule dump in 2 ticks - we need to delay this to wait for onChatMessage handler which updates mvp \u0026 min contribution\n          log.debug(\"Scheduling finished Nex fight dump in {} ticks\", DUMP_RESULTS_TICKS);\n          ticksUntilDumpResults \u003d DUMP_RESULTS_TICKS;\n          ticksUntilOverlayRemoval \u003d REMOVE_OVERLAY_TICKS;\n        } else if (newStatus \u003d\u003d 1 || newStatus \u003d\u003d 0) {\n          // Ignore 2 -\u003e 1 and 2 -\u003e 0 statuses since this usually means player is leaving after a finished fight\n          if (lastNexStatus !\u003d 2) {\n            // Either player left arena while Nex was alive (3 -\u003e 1) or player logged out (X -\u003e 0)\n            // Cancel fight status immediately but schedule overlay removal to be done later\n            panel.finishCurrentRun(RunState.CANCELLED, 0);\n            log.debug(\"Player left Nex fight, scheduling overlay removal in {} ticks\", REMOVE_OVERLAY_TICKS);\n            ticksUntilOverlayRemoval \u003d REMOVE_OVERLAY_TICKS;\n          }\n        }\n      }\n      // Store previous value of Nex\u0027s status\n      lastNexStatus \u003d event.getValue();\n    }\n  }\n\n  @Subscribe\n  public void onGameTick(GameTick tick) {\n    // Player is currently in a Nex fight - update data of the ongoing fight each game tick\n    if (isNexAlive() \u0026\u0026 panel.isCurrentRunActive()) {\n      int playerCount \u003d (int) client.getTopLevelWorldView().players().stream().count();\n      panel.updateCurrentRun(ownContribution, totalContribution, playerCount);\n      // Reset tick contributions\n      ownContribution \u003d 0;\n      totalContribution \u003d 0;\n      return;\n    }\n\n    // Player is not in a Nex fight - delay executions by decrementing values until they reach 0\n    if (ticksUntilFightStart \u003e -1) {\n      if (ticksUntilFightStart \u003d\u003d 0) {\n        // Only actually start if the player is still at Nex (they could\u0027ve left since the delay start)\n        if (isNexAlive()) {\n          startFight();\n        }\n      }\n      ticksUntilFightStart--;\n    }\n\n    if (ticksUntilDumpResults \u003e -1) {\n      if (ticksUntilDumpResults \u003d\u003d 0) {\n        log.debug(\"Dumping finished Nex fight results\");\n        panel.finishCurrentRun(RunState.FINISHED, DUMP_RESULTS_TICKS);\n        logKillToFile(panel.currentRun);\n      }\n      ticksUntilDumpResults--;\n    }\n\n    if (ticksUntilOverlayRemoval \u003e -1) {\n      if (ticksUntilOverlayRemoval \u003d\u003d 0) {\n        log.debug(\"30 seconds passed since leaving Nex, removing overlay\");\n        overlay.setShouldRender(false);\n      }\n      ticksUntilOverlayRemoval--;\n    }\n  }\n\n  @Subscribe\n  public void onChatMessage(ChatMessage event) {\n    if (event.getType() !\u003d ChatMessageType.GAMEMESSAGE \u0026\u0026 event.getType() !\u003d ChatMessageType.NPC_SAY) {\n      return;\n    }\n    String message \u003d event.getMessage();\n\n    // Use Nex\u0027s static lines to determine start of the fight. This solution is better than\n    // detecting hitsplats - Nex could be not attacked and her first damaging attack is delayed\n    if (message.equals(\"Nex|Fill my soul with smoke!\")) {\n      // Nex fight will actually start in a few ticks, delay until that exact moment\n      log.debug(\"Scheduling fight start in {} ticks\", FIGHT_START_DELAY_TICKS);\n      ticksUntilFightStart \u003d FIGHT_START_DELAY_TICKS;\n    }\n\n    if (message.equals(\"You were the MVP for this fight!\")) {\n      panel.setCurrentRunMvp();\n      log.debug(\"Nex MVP message detected\");\n    }\n    // Checking for Nex-specific drop message ensures we don\u0027t faultily assume player qualifying\n    // for minimal contribution in the event of someone in the arena receiving a unique drop\n    if (message.startsWith(\"\u003ccol\u003def0083\u003eNex dropped:\")) {\n      panel.setCurrentRunMinimumContribution();\n      log.debug(\"Nex drop message detected\");\n    }\n\n  }\n\n  @Subscribe\n  public void onHitsplatApplied(HitsplatApplied event) {\n    if (!panel.isCurrentRunActive()) {\n      return;\n    }\n\n    if (event.getActor() instanceof NPC) {\n      Hitsplat hitsplat \u003d event.getHitsplat();\n      if (hitsplat.isMine()) {\n        ownContribution +\u003d hitsplat.getAmount();\n        //log.debug(\"Hitsplat applied to me: {}\", hitsplat.getAmount()); // very spammy\n      }\n      if (hitsplat.getHitsplatType() !\u003d HitsplatID.HEAL) {\n        totalContribution +\u003d hitsplat.getAmount();\n        //log.debug(\"Total contribution updated: {}\", totalContribution); // very spammy\n      }\n    }\n  }\n\n  private void logKillToFile(NexDroprateCalculatorRun run) {\n    if (!config.logKills()) {\n      return;\n    }\n    Player player \u003d client.getLocalPlayer();\n    if (player \u003d\u003d null) {\n      return;\n    }\n\n    String timestamp \u003d LocalDateTime.now().format(TIMESTAMP_FORMAT);\n    String killTime \u003d Utils.formatDuration(run.getTicks());\n    String contribution \u003d String.format(\"%.2f%%\", run.getContributionPercent());\n    String avgPlayers \u003d String.format(\"%.2f\", run.calculateAvgPlayers());\n\n    String line \u003d String.join(\",\", timestamp, killTime, contribution, avgPlayers);\n\n    executor.execute(() -\u003e {\n      // First, ensure subdirectories for plugin and user exist\n      Path path \u003d null;\n      try {\n        path \u003d Files.createDirectories(Paths.get(RuneLite.RUNELITE_DIR.getPath(), NexDroprateCalculatorConfig.GROUP, player.getName()));\n      } catch (IOException e) {\n        log.error(\"Failed to create directories\");\n        return; // early out and don\u0027t attempt to write to log file\n      }\n\n      boolean logFileExists \u003d Files.exists(path.resolve(KILL_LOG_FILE_NAME));\n      try (PrintWriter writer \u003d new PrintWriter(new FileWriter(path.resolve(KILL_LOG_FILE_NAME).toString(), true))) {\n        if (!logFileExists) {\n          writer.println(\"timestamp,kill_time,contribution,avg_players\");\n        }\n        writer.println(line);\n        log.debug(\"Logged kill to file: {}\", line);\n      } catch (IOException e) {\n        log.error(\"Failed to write to kill log\", e);\n      }\n    });\n  }\n\n  @Provides\n  NexDroprateCalculatorConfig provideConfig(ConfigManager configManager) {\n    return configManager.getConfig(NexDroprateCalculatorConfig.class);\n  }\n}","filePath":"src/main/java/com/nexdropratecalculator/NexDroprateCalculatorPlugin.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"Utils.java","content":"package com.nexdropratecalculator;\n\npublic class Utils {\n     static String fraction(int value) {\n        if (value \u003d\u003d 0) {\n            return \"N/A\";\n        }\n        return \"1/\" + value;\n    }\n\n    static String formatDuration(int ticks) {\n        double secondsTotal \u003d ticks * 0.6;\n        double seconds \u003d secondsTotal % 60.0;\n        int minutes \u003d (int) Math.floor((secondsTotal % 3600.0) / 60.0);\n        int hours \u003d (int) Math.floor(secondsTotal / 3600.0);\n        return String.format(\"%02dh %02dm %04.1fs\", hours, minutes, seconds);\n    }\n}","filePath":"src/main/java/com/nexdropratecalculator/Utils.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"NexDroprateCalculatorOverlay.java","content":"package com.nexdropratecalculator;\n\nimport static net.runelite.api.MenuAction.RUNELITE_OVERLAY_CONFIG;\nimport static net.runelite.client.ui.overlay.OverlayManager.OPTION_CONFIGURE;\n\nimport java.awt.Color;\nimport java.awt.Dimension;\nimport java.awt.Graphics2D;\nimport javax.inject.Inject;\n\nimport net.runelite.client.ui.overlay.OverlayPanel;\nimport net.runelite.client.ui.overlay.OverlayPosition;\nimport net.runelite.client.ui.overlay.components.LineComponent;\nimport net.runelite.client.ui.overlay.components.TitleComponent;\nimport lombok.Getter;\nimport lombok.Setter;\n\npublic class NexDroprateCalculatorOverlay extends OverlayPanel {\n    private final NexDroprateCalculatorPlugin plugin;\n    private final NexDroprateCalculatorConfig config;\n    @Getter @Setter\n    private boolean shouldRender \u003d false;\n\n    @Inject\n    public NexDroprateCalculatorOverlay(NexDroprateCalculatorPlugin plugin, NexDroprateCalculatorConfig config) {\n        super(plugin);\n        this.setPosition(OverlayPosition.TOP_LEFT);\n        // Add an option to Shift+Right click\u0027s context menu which can take the user to plugin settings\n        addMenuEntry(RUNELITE_OVERLAY_CONFIG, OPTION_CONFIGURE, \"Configure Nex Droprate Calculator\");\n        this.plugin \u003d plugin;\n        this.config \u003d config;\n    }\n\n    @Override\n    public Dimension render(Graphics2D graphics) {\n        if (!config.showOverlay() || !shouldRender) {\n            return null;\n        }\n        NexDroprateCalculatorPanel panel \u003d plugin.panel;\n        if (panel \u003d\u003d null) {\n            return null;\n        }\n        NexDroprateCalculatorRun run \u003d panel.currentRun;\n\n        // Title\n        panelComponent.getChildren().add(TitleComponent.builder().text(\"Nex Calculator\").color(Color.ORANGE).build());\n\n        // Drop rate\n        panelComponent.getChildren().add(LineComponent.builder()\n                .left(\"Drop Rate:\").right(Utils.fraction(run.getUniqueChanceRoll()))\n                .leftColor(Color.WHITE).rightColor(Color.WHITE).build());\n        // Contribution\n        double contributionPercent \u003d run.getContributionPercent();\n        double avgPlayers \u003d run.calculateAvgPlayers();\n        double threshold \u003d avgPlayers \u003e 0 ? 100.0 / avgPlayers : 0; // Threshold determines qualifying for equal damage / mvp\n        panelComponent.getChildren().add(LineComponent.builder()\n                .left(\"Contribution:\").right(String.format(\"%.1f%%\", contributionPercent))\n                .leftColor(Color.WHITE).rightColor(contributionPercent \u003e\u003d threshold ? Color.GREEN : Color.RED).build());\n        // Players\n        final int playerCount \u003d run.getPlayersCurrent();\n        String playersValue \u003d String.format(\"%d\", playerCount);\n        // Someone might have left after the fight started, account for that and display original player count as well\n        if (playerCount \u003c run.getPlayersMax()) {\n            playersValue \u003d String.format(\"%d out of %d\", playerCount, run.getPlayersMax());\n        }\n        panelComponent.getChildren().add(LineComponent.builder()\n                .left(\"Players:\").right(playersValue)\n                .leftColor(Color.WHITE).rightColor(Color.WHITE).build());\n\n        return super.render(graphics);\n    }\n}","filePath":"src/main/java/com/nexdropratecalculator/NexDroprateCalculatorOverlay.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"NexDroprateCalculatorPanel.java","content":"package com.nexdropratecalculator;\n\nimport java.awt.*;\nimport javax.swing.*;\nimport javax.swing.border.EmptyBorder;\n\nimport lombok.Data;\nimport lombok.Getter;\nimport lombok.Setter;\nimport net.runelite.client.ui.ColorScheme;\nimport net.runelite.client.ui.FontManager;\nimport net.runelite.client.ui.PluginPanel;\n\nclass Formatter {\n  private static final String HTML_TEMPLATE \u003d \"\u003chtml\u003e\u003cdiv style\u003d\u0027color:white;font-size:10px;\u0027\u003e%s\u003c/div\u003e\u003c/html\u003e\";\n\n  public static String text(String text) { return String.format(HTML_TEMPLATE, text); }\n  public static String value(String name, String value) { return String.format(HTML_TEMPLATE, \"\u003cb\u003e\" + name + \":\u003c/b\u003e \" + value); }\n\n  // Common formats\n  public static String formatUniqueChance(double percent) { return value(\"Unique Chance\", String.format(\"%.4f%%\", percent)); }\n  public static String formatUniqueRoll(int roll) { return value(\"Unique Roll\", Utils.fraction(roll)); }\n  public static String formatContribution(double percent) { return value(\"Contribution\", String.format(\"%.2f%%\", percent)); }\n  public static String formatDamage(int own, int total) { return value(\"Damage\", own + \"/\" + total); }\n  public static String formatPlayers(double avg) { return value(\"Players\", String.format(\"%.2f\", avg)); }\n  public static String formatMVP(boolean isMVP) { return value(\"MVP\", isMVP ? \"yes\" : \"no\"); }\n  public static String formatTime(int ticks) { return value(\"Time\", Utils.formatDuration(ticks)); }\n  public static String formatState(RunState state) { return value(\"State\", state.toString()); }\n\n  // Formats for averages\n  public static String formatTotalKills(int total) { return value(\"Total Kills\", String.valueOf(total)); }\n  public static String formatMVPPercent(double percent) { return value(\"MVP\", String.format(\"%.2f%%\", percent * 100)); }\n  public static String formatMVPTotal(int total) { return value(\"MVP Total\", String.valueOf(total)); }\n  public static String formatKillsPerHour(double killsPerHour) { return value(\"Kills / hr\", String.format(\"%.2f\", killsPerHour)); }\n  public static String formatUniqueChancePerHour(double percent) { return value(\"Unique Chance / hr\", String.format(\"%.4f%%\", percent)); }\n  public static String formatCombinedUniqueRoll(int totalKills, int uniqueChanceRoll) {\n    int combinedRoll \u003d totalKills !\u003d 0 ? uniqueChanceRoll / totalKills : 0;\n    return value(\"Combined Unique Roll\", Utils.fraction(combinedRoll));\n  }\n}\n\nabstract class DisplaySection {\n  protected final JPanel parent;\n  protected final Font font;\n\n  public DisplaySection(JPanel parent, Font font, String sectionTitle) {\n    this.parent \u003d parent;\n    this.font \u003d font;\n\n    JLabel titleLabel \u003d new JLabel(Formatter.text(String.format(\"- %s -\", sectionTitle)));\n    titleLabel.setFont(font);\n    this.parent.add(titleLabel);\n  }\n\n  protected JLabel createLabel() {\n    JLabel label \u003d new JLabel();\n    label.setFont(font);\n    parent.add(label);\n    return label;\n  }\n}\n\nclass RunDisplaySection extends DisplaySection {\n  private final JLabel uniqueChanceLabel \u003d createLabel();\n  private final JLabel uniqueRollLabel \u003d createLabel();\n  private final JLabel contributionPercentLabel \u003d createLabel();\n  private final JLabel contributionFlatLabel \u003d createLabel();\n  private final JLabel playersLabel \u003d createLabel();\n  private final JLabel isMVPLabel \u003d createLabel();\n  private final JLabel timeLabel \u003d createLabel();\n  private JLabel stateLabel \u003d null;\n\n  public RunDisplaySection(JPanel parent, Font font, String sectionTitle, boolean includeState) {\n    super(parent, font, sectionTitle);\n\n    if (includeState) {\n      this.stateLabel \u003d createLabel();\n    }\n    createLabel(); // TODO: consider getting rid of empty labels and instead implement margin\n  }\n\n  public void updateFromRun(NexDroprateCalculatorRun run) {\n    uniqueChanceLabel.setText(Formatter.formatUniqueChance(run.getUniqueChancePercent()));\n    uniqueRollLabel.setText(Formatter.formatUniqueRoll(run.getUniqueChanceRoll()));\n    contributionPercentLabel.setText(Formatter.formatContribution(run.getContributionPercent()));\n    contributionFlatLabel.setText(Formatter.formatDamage(run.getContributionFlatOwn(), run.getContributionFlatTotal()));\n    playersLabel.setText(Formatter.formatPlayers(run.calculateAvgPlayers()));\n    isMVPLabel.setText(Formatter.formatMVP(run.isMVP()));\n    timeLabel.setText(Formatter.formatTime(run.getTicks()));\n    if (stateLabel !\u003d null) {\n      stateLabel.setText(Formatter.formatState(run.getState()));\n    }\n  }\n}\n\nclass AverageDisplaySection extends DisplaySection {\n  private final JLabel totalKillsLabel \u003d createLabel();\n  private final JLabel averageUniqueChanceLabel \u003d createLabel();\n  private final JLabel averageUniqueRollLabel \u003d createLabel();\n  private final JLabel averageContributionPercentLabel \u003d createLabel();\n  private final JLabel averageContributionFlatLabel \u003d createLabel();\n  private final JLabel averagePlayersLabel \u003d createLabel();\n  private final JLabel averageMVPPercentLabel \u003d createLabel();\n  private final JLabel averageMVPTotalLabel \u003d createLabel();\n  private final JLabel averageTimeLabel \u003d createLabel();\n  private final JLabel killsPerHourLabel \u003d createLabel();\n  private final JLabel uniqueChancePerHourLabel \u003d createLabel();\n  private final JLabel uniqueRollCombinedLabel \u003d createLabel();\n  // Button is added later inside the init function\n\n  public AverageDisplaySection(JPanel parent, Font font, String sectionTitle) {\n    super(parent, font, sectionTitle);\n  }\n\n  public void updateFromAverage(AverageValues avg) {\n    totalKillsLabel.setText(Formatter.formatTotalKills(avg.getTotalKills()));\n    averageUniqueChanceLabel.setText(Formatter.formatUniqueChance(avg.getUniqueChancePercent()));\n    averageUniqueRollLabel.setText(Formatter.formatUniqueRoll(avg.getUniqueChanceRoll()));\n    averageContributionPercentLabel.setText(Formatter.formatContribution(avg.getContributionPercent()));\n    averageContributionFlatLabel.setText(Formatter.formatDamage(avg.getContributionFlatOwn(), avg.getContributionFlatTotal()));\n    averagePlayersLabel.setText(Formatter.formatPlayers(avg.getPlayersAverage()));\n    averageMVPPercentLabel.setText(Formatter.formatMVPPercent(avg.getMvpPercent()));\n    averageMVPTotalLabel.setText(Formatter.formatMVPTotal(avg.getMvpTotal()));\n    averageTimeLabel.setText(Formatter.formatTime(avg.getAvgTicksPerKill()));\n    killsPerHourLabel.setText(Formatter.formatKillsPerHour(avg.getRunsPerHour()));\n    uniqueChancePerHourLabel.setText(Formatter.formatUniqueChancePerHour(avg.getUniqueChancePercentPerHour()));\n    uniqueRollCombinedLabel.setText(Formatter.formatCombinedUniqueRoll(avg.getTotalKills(), avg.getUniqueChanceRoll()));\n  }\n}\n\n@Data\nclass AverageValues {\n  private int totalKills;\n  private int avgTicksPerKill;\n  private double uniqueChancePercent;\n  private int uniqueChanceRoll;\n  private double contributionPercent;\n  private int contributionFlatOwn;\n  private int contributionFlatTotal;\n  private int minimumContributionTotal;\n  private double playersAverage;\n  private int mvpTotal;\n  private double mvpPercent;\n\n  public double getRunsPerHour() {\n    if (avgTicksPerKill \u003d\u003d 0) {\n      return 0;\n    }\n    return 6000.0 / (avgTicksPerKill + 61.0); // From Nex dying, there\u0027s exactly 61 ticks until she spawns and starts new fight\n  }\n\n  public double getUniqueChancePercentPerHour() {\n    return (1 - Math.pow(1.0 - (uniqueChancePercent / 100.0), getRunsPerHour())) * 100.0;\n  }\n\n  public void addRun(NexDroprateCalculatorRun run) {\n    int newTotal \u003d totalKills + 1;\n\n    avgTicksPerKill \u003d (int) Math.round((avgTicksPerKill * totalKills + run.getTicks()) / (double) newTotal);\n\n    uniqueChancePercent \u003d (uniqueChancePercent * totalKills + run.getUniqueChancePercent()) / newTotal;\n    uniqueChanceRoll \u003d uniqueChancePercent \u003e 0 ? (int) Math.ceil(100.0 / uniqueChancePercent) : 0;\n\n    contributionPercent \u003d (contributionPercent * totalKills + run.getContributionPercent()) / newTotal;\n\n    contributionFlatOwn \u003d (int) Math.round((contributionFlatOwn * totalKills + run.getContributionFlatOwn()) / (double) newTotal);\n    contributionFlatTotal \u003d (int) Math.round((contributionFlatTotal * totalKills + run.getContributionFlatTotal()) / (double) newTotal);\n    if (run.isMinimumContribution()) minimumContributionTotal++;\n\n    playersAverage \u003d (playersAverage * totalKills + run.calculateAvgPlayers()) / newTotal;\n\n    if (run.isMVP()) mvpTotal++;\n    mvpPercent \u003d mvpTotal / (double) newTotal;\n\n    totalKills++;\n  }\n}\n\npublic class NexDroprateCalculatorPanel extends PluginPanel {\n  protected NexDroprateCalculatorRun currentRun \u003d new NexDroprateCalculatorRun(RunState.NOT_STARTED);\n  private NexDroprateCalculatorRun lastRun \u003d new NexDroprateCalculatorRun(RunState.NOT_STARTED);\n  private AverageValues avg \u003d new AverageValues();\n\n  @Getter @Setter\n  private boolean currentRunActive \u003d false;\n\n  private RunDisplaySection currentSection;\n  private RunDisplaySection lastSection;\n  private AverageDisplaySection averageSection;\n\n  void init() {\n    final Font font \u003d FontManager.getDefaultFont();\n\n    setLayout(new BorderLayout());\n    setBackground(ColorScheme.DARK_GRAY_COLOR);\n    setBorder(new EmptyBorder(10, 10, 10, 10));\n\n    // Run panel\n    JPanel runPanel \u003d new JPanel();\n    runPanel.setBackground(ColorScheme.DARKER_GRAY_COLOR);\n    runPanel.setBorder(new EmptyBorder(5, 5, 5, 5));\n    runPanel.setLayout(new GridLayout(0, 1));\n\n    currentSection \u003d new RunDisplaySection(runPanel, font, \"Current Kill\", true);\n    lastSection \u003d new RunDisplaySection(runPanel, font, \"Last Kill\", false);\n    averageSection \u003d new AverageDisplaySection(runPanel, font, \"Averages\");\n\n    JButton resetButton \u003d new JButton(Formatter.text(\"Reset Averages\"));\n    resetButton.setFont(font);\n    resetButton.setFocusable(false);\n    resetButton.addActionListener(e -\u003e {\n      avg \u003d new AverageValues();\n      averageSection.updateFromAverage(avg);\n    });\n    runPanel.add(resetButton);\n\n    add(runPanel);\n    updateAllSections(); // Run once to initialize displays with default values\n  }\n\n  private void updateAllSections() {\n    currentSection.updateFromRun(currentRun);\n    lastSection.updateFromRun(lastRun);\n    averageSection.updateFromAverage(avg);\n  }\n\n  // A new run started, initialize values\n  public void startCurrentRun(int playerCount) {\n    currentRunActive \u003d true;\n    // Set what\u0027s currently stored at currentRun into lastRun, but only if that run was completed successfully\n    if (currentRun.getState() \u003d\u003d RunState.FINISHED) {\n      lastRun \u003d currentRun;\n    }\n    currentRun \u003d new NexDroprateCalculatorRun(RunState.STARTED);\n    currentRun.setPlayersCurrent(playerCount);\n    currentRun.setPlayersMax(playerCount);\n    updateAllSections();\n  }\n\n  // Current run is ongoing, updating run data with recorded values\n  public void updateCurrentRun(int ownContribution, int totalContribution, int players) {\n    currentRun.updateRun(ownContribution, totalContribution, players);\n    currentSection.updateFromRun(currentRun);\n  }\n\n  // Current run either just finished or was cancelled (due to player leaving/dying)\n  public void finishCurrentRun(RunState finishReason, int ticksToSubtract) {\n    currentRunActive \u003d false;\n\n    if (finishReason \u003d\u003d RunState.FINISHED) {\n      // Current run finished successfully\n      if (currentRun.isMVP()) {\n        // Set the mvp bonus - Math.min to ensure contribution doesn\u0027t exceed 100% (in case of a solo run)\n        currentRun.updateUniqueChance(Math.min(currentRun.getContributionPercent() * 1.1, 100.0));\n      }\n      // HACK: We\u0027ve waited for N ticks after fight ended, correct the relevant values\n      currentRun.adjustAfterFinish(ticksToSubtract);\n      // update average run values from currentRun\n      currentRun.setState(finishReason);\n      avg.addRun(currentRun);\n    } else if (finishReason \u003d\u003d RunState.CANCELLED \u0026\u0026 currentRun.getState() !\u003d RunState.FINISHED) {\n      // Player has left before the fight has finished - only consider run as cancelled if previously set state wasn\u0027t set to finished\n      currentRun.setState(finishReason);\n    }\n    updateAllSections();\n  }\n\n  public void setCurrentRunMvp() {\n    currentRun.setMVP(true);\n  }\n\n  public void setCurrentRunMinimumContribution() {\n    currentRun.setMinimumContribution(true);\n  }\n}","filePath":"src/main/java/com/nexdropratecalculator/NexDroprateCalculatorPanel.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"NexDroprateCalculatorConfig.java","content":"package com.nexdropratecalculator;\n\nimport net.runelite.client.config.Config;\nimport net.runelite.client.config.ConfigGroup;\nimport net.runelite.client.config.ConfigItem;\n\n@ConfigGroup(NexDroprateCalculatorConfig.GROUP)\npublic interface NexDroprateCalculatorConfig extends Config {\n    String GROUP \u003d \"nex-droprate-calculator\";\n\n    String SHOW_OVERLAY \u003d \"showOverlay\";\n    String DROP_RATE \u003d \"DropRate\";\n    String LOG_KILLS \u003d \"logKills\";\n\n    @ConfigItem(\n            position \u003d 1,\n            keyName \u003d SHOW_OVERLAY,\n            name \u003d \"Show Overlay\",\n            description \u003d \"Toggle overlay\"\n    )\n    default boolean showOverlay() {\n        return true;\n    }\n\n    @ConfigItem(\n            position \u003d 2,\n            keyName \u003d DROP_RATE,\n            name \u003d \"Increase Drop Rate\",\n            description \u003d \"Only works if you believe!\"\n    )\n    default boolean DropRate() {\n        return true;\n    }\n\n    @ConfigItem(\n            position \u003d 3,\n            keyName \u003d LOG_KILLS,\n            name \u003d \"Log Kills\",\n            description \u003d \"Log kills data to the nex-droprate-calculator folder within the .runelite directory\"\n    )\n    default boolean logKills() {\n        return false;\n    }\n}\n\n","filePath":"src/main/java/com/nexdropratecalculator/NexDroprateCalculatorConfig.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"NexDroprateCalculatorRun.java","content":"package com.nexdropratecalculator;\n\nimport lombok.Data;\n\n@Data\npublic class NexDroprateCalculatorRun {\n  private RunState state;\n\n  private int ticks;\n  private double uniqueChancePercent;\n  private int uniqueChanceRoll;\n  private double contributionPercent;\n  private int contributionFlatOwn;\n  private int contributionFlatTotal;\n  private int playersMax;           // amount of players who were present at the start of the kill\n  private int playersCurrent;       // amount of players currently in the room (can only be lower than the max)\n  private int playersWeightedValue; // stores sum of players present each tick for weighted player count calculation\n  private boolean isMinimumContribution;\n  private boolean isMVP;\n\n  public NexDroprateCalculatorRun(RunState stateToSet) {\n    this.state \u003d stateToSet;\n  }\n\n  public void updateRun(int ownContribution, int totalContribution, int players) {\n    ticks++;\n\n    playersCurrent \u003d players;\n    playersWeightedValue +\u003d players;\n\n    // Add to own and total contribution values\n    contributionFlatOwn +\u003d ownContribution;\n    contributionFlatTotal +\u003d totalContribution;\n\n    if (contributionFlatOwn \u003e 0 \u0026\u0026 contributionFlatTotal \u003e 0) {\n      contributionPercent \u003d (double) contributionFlatOwn / contributionFlatTotal * 100.0;\n      updateUniqueChance(contributionPercent);\n    }\n  }\n\n  // After run finishes, we always wait few ticks before actually stopping it - this function accounts for that\n  public void adjustAfterFinish(int ticksToSubtract) {\n    ticks -\u003d ticksToSubtract;\n    playersWeightedValue -\u003d playersCurrent * ticksToSubtract; // player count can\u0027t change within ticks after nex dies\n  }\n\n  public void updateUniqueChance(double contribPercent) {\n    uniqueChanceRoll \u003d (int) Math.ceil(43.0 * (100.0 / contribPercent));\n    uniqueChancePercent \u003d 1.0 / uniqueChanceRoll * 100.0;\n  }\n\n  public double calculateAvgPlayers() {\n    if (ticks \u003d\u003d 0) {\n      return 0;\n    }\n    return (double) playersWeightedValue / ticks;\n  }\n}","filePath":"src/main/java/com/nexdropratecalculator/NexDroprateCalculatorRun.java","pluginName":"nex-droprate-calculator","lineOffsets":[]},{"fileName":"RunState.java","content":"package com.nexdropratecalculator;\n\nimport lombok.AllArgsConstructor;\n\n@AllArgsConstructor\npublic enum RunState {\n    // Fight started\n    STARTED(\"Active\"),\n    // Fight was started earlier and just concluded\n    FINISHED(\"Finished\"),\n    // Fight was ongoing but player left\n    // Cancelled runs will be ignored from adding into the average and being set to lastRun\n    CANCELLED(\"Cancelled\"),\n    // Fight has not been started yet (default state for object initialization)\n    NOT_STARTED(\"Not Started\");\n\n    final String displayText;\n\n    @Override\n    public String toString() {\n        return this.displayText;\n    }\n}","filePath":"src/main/java/com/nexdropratecalculator/RunState.java","pluginName":"nex-droprate-calculator","lineOffsets":[]}]}